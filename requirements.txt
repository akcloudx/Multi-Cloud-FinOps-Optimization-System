streamlit>=1.37
pandas>=2.0
numpy>=1.26
# Pinned exactly, not >= , per Microsoft's own guidance: the mssql-python
# SQLAlchemy dialect (below) only exists from 2.1.0b2 (a pre-release) - an
# unpinned >=2.0 could resolve to a stable 2.0.x that's missing the dialect
# entirely, or drift to a later, untested 2.1 beta. Tried 2026-08 for
# Managed Identity / passwordless Azure SQL auth - KNOWN RISK, Microsoft's
# docs explicitly say not to use this pre-release "for production systems
# with strict stability requirements." See db/schema.py's get_engine() for
# the full reasoning and the pre-managed-identity-backup git branch if this
# needs to be rolled back to the stable sqlalchemy 2.0.x + password-based
# DATABASE_URL path.
sqlalchemy==2.1.0b2
plotly>=5.18
boto3>=1.34
botocore>=1.34
python-dotenv>=1.0
requests>=2.31.0
bcrypt>=4.0.0
pyodbc>=5.0
pymssql>=2.2.0
mssql-python>=1.12.0
azure-identity>=1.15.0
azure-mgmt-resourcegraph>=8.0.0
azure-mgmt-resource>=23.0.0
azure-mgmt-subscription>=3.1.1
azure-mgmt-authorization>=4.0.0
azure-mgmt-reservations>=3.0.0
azure-mgmt-billingbenefits>=1.0.0b2
azure-mgmt-datafactory>=10.0.0
# Added for VM Rightsizing in production (2026-09-01, mentor feedback) -
# azure-mgmt-compute's resource_skus.list() gets each VM SKU's total RAM
# (needed to convert Azure Monitor's raw-bytes memory metric to %), and
# azure-mgmt-monitor's metrics.list() is the real live CPU/memory
# utilization fetch itself. See azure_conn/connector.py::
# fetch_vm_sku_memory / fetch_vm_utilization_metrics.
azure-mgmt-compute>=30.0.0
azure-mgmt-monitor>=6.0.0
cryptography<44,>=42